<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .modal {
            max-height: 100vh;

            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.5);
        }

        /* .modal-content {
            background-color: #fefefe;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 50%;
        } */
        .modal-content {
            background-color: #fefefe;
            margin: auto;
            padding: 20px;
            border: 1px solid #888;
            width: 50%;
            max-height: 100vh;
            overflow-y: auto;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

        h2 {
            margin-bottom: 1rem;
            text-align: center;
            font-size: 1.8rem;
            text-transform: capitalize;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .input-field {
            display: flex;
            margin-bottom: 1rem;
        }

        .input-field label {
            width: 30%;
            font-size: 1.2rem;
        }

        .input-field input {
            width: 70%;
            padding: 6px 10px;
            font-size: 1rem;
        }

        #save-change-button {
            background-color: #4caf50;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1.2rem;
            cursor: pointer;
        }

        #save-change-button:hover {
            opacity: 0.8;
        }
    </style>
</head>
<div id="modal" class="modal">
    <div class="modal-content">
        <span class="close" onclick="closePopup()">&times;</span>
        <h2>Update Book</h2>

        <form action="/update/<%= user._id %>" method="post">
            <div class="input-field">
                <label for="title">Title:</label>
                <input type="text" id="updateTitle" value="<%= user.title%>" name="title" placeholder=" Enter title"
                    required>
            </div>
            <div class="input-field">
                <label for="updateAuthor">Author:</label>
                <input type="text" id="updateAuthor" value="<%= user.author %>" name="author" placeholder="Enter author"
                    required>
            </div>
            <div class="input-field">
                <label for="updatePrice">Price:</label>
                <input type="text" id="updatePrice" value="<%= user.price %>" name="price" placeholder="Enter price"
                    required>
            </div>
            <div class="input-field">
                <label for="updateCategory">Category:</label>
                <input type="text" id="updateCategory" value="<%= user.category %>" name="category"
                    placeholder="Enter category" required>
            </div>
            <button type="submit" id="save-change-button">Save Changes</button>
        </form>
    </div>
</div>

<body>

</body>

</html>